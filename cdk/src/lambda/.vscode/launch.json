{
    "version": "0.2.0",
    "configurations": [
        
        {
            "type": "node",
            "request": "launch",
            "name": "デバッグ実行",
            "program": "${workspaceRoot}/.vscode/drive.js",
            "cwd": "${workspaceRoot}/",
            "outFiles": [ "${workspaceRoot}/*.js" ], 
            "env": {
                "TZ": "Asia/Tokyo",
                "BUCKET_NAME": "test-00000001",
                "IsLocal": "Yes"

            },
            "args": [
                "event.json",
            ]
        }
    ]
}

